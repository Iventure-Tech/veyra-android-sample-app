<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- One crimson accent across all three surfaces (replaces the original purple/teal),
         so black + white-on-black (SoftPOS) + deep-red cards (Wallet) read as one product. -->

    <!-- Get paid (SoftPOS) flow — white-on-black pages (from layout drawables) + crimson accent. -->
    <style name="Theme.SoftPOS" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/accent_red</item>
        <item name="colorPrimaryVariant">@color/accent_red_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/accent_red</item>
        <item name="colorSecondaryVariant">@color/accent_red_dark</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:statusBarColor">@color/bank_bg</item>
    </style>

    <!-- Pay (wallet) flow — black background, deep-red cards + crimson accent. -->
    <style name="Theme.VeyraWallet" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/accent_red</item>
        <item name="colorPrimaryVariant">@color/accent_red_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/accent_red</item>
        <item name="colorSecondaryVariant">@color/accent_red_dark</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:statusBarColor">@color/bank_bg</item>
    </style>
    <!-- NoActionBar variant for activities that use a custom Toolbar (e.g. TransactionHistoryActivity) -->
    <style name="Theme.VeyraWallet.NoActionBar" parent="Theme.VeyraWallet">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Neo-bank home — pure black, crimson accent -->
    <style name="Theme.VeyraBank" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/accent_red</item>
        <item name="colorPrimaryVariant">@color/accent_red_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/accent_red</item>
        <item name="android:statusBarColor">@color/bank_bg</item>
        <item name="android:windowBackground">@color/bank_bg</item>
    </style>
</resources>
